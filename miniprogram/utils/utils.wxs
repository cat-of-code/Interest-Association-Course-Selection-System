var util={
  date2num:function(activity_date){
    var num2 = parseInt(activity_date.slice(0,4))*10000+parseInt(activity_date.slice(5,7))*100+parseInt(activity_date.slice(8,10))
    return num2
  },
}
module.exports = {   
  date2num: util.date2num,
}
